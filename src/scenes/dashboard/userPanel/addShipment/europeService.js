import { Shippo } from 'shippo'; //npm package

const shippo = new Shippo({
  apiKeyHeader: "your_api_key_here",
});

const saveShipment = async () => {
  // Function to create a shipment using Shippo's API for europe 
};
